{
  "name": "heyalyson-api",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "author": "AnsonErvin Inc.",
  "engines": {
    "node": "20.x"
  },
  "cacheDirectories": [
    "node_modules"
  ],
  "scripts": {
    "build": "tsc",
    "heroku-postbuild": "npm run build",
    "test": "NODE_ENV=test jest --passWithNoTests --runInBand",
    "test:watch": "NODE_ENV=test jest --watch",
    "dev": "export NODE_ENV=dev && npm run build && nodemon ./index.ts ",
    "debug-wipe": "NODE_ENV=debug && npm run build-test  && npm run build && nodemon ./index.ts",
    "start": "NODE_ENV=production && ts-node ./dist/index.js",
    "tsc": "tsc",
    "postinstall": "npm run tsc"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.21.1",
    "bunyan": "^1.8.15",
    "dotenv": "^16.4.5",
    "firebase": "^12.3.0",
    "firebase-admin": "^13.5.0",
    "jest": "^30.2.0",
    "restify": "^11.0.0",
    "restify-cors-middleware2": "^2.2.1",
    "restify-errors": "^8.0.2",
    "restify-router": "^0.6.2",
    "ts-node": "^10.7.0",
    "typescript": "^4.6.3"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/restify": "^8.5.1",
    "@types/restify-errors": "^4.3.3",
    "jest": "^30.2.0",
    "mocha": "^8.2.1",
    "nodemon": "^2.0.22",
    "ts-jest": "29.4.4"
  }
}
